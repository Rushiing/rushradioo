# Content

This folder can be opened as an Obsidian vault.

- `blogs/`: longform Markdown or MDX posts
- `feeds/`: short notes and knowledge cards
- `_templates/`: starter templates for Obsidian

Only files with `status: published` or no `status` field are shown on the site.
Use `status: draft` for private drafts.
